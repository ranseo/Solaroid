<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="viewModel"
            type="com.example.solaroid.solaroiddetail.SolaroidDetailViewModel" />

        <import type="android.view.View"/>

    </data>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".solaroiddetail.SolaroidDetailFragment">


        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/back_layout"
            android:layout_width="300dp"
            android:layout_height="600dp"
            android:layout_gravity="center"
            android:background="@drawable/photo_ticket_border_line"
            app:layout_constraintVertical_bias="0.412"
            android:visibility="@{viewModel.spinBackSide ? View.VISIBLE : View.GONE}">
            <TextView
                android:id="@+id/solaroid_text_back"
                android:text="@{viewModel.photoTicket.backText}"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:textAlignment="textStart"
                android:textColor="@color/black"
                android:layout_marginLeft="24dp"
                android:layout_marginTop="28dp"
                android:layout_marginRight="24dp"
                android:layout_marginBottom="28dp"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintLeft_toLeftOf="parent"

                />

        </androidx.constraintlayout.widget.ConstraintLayout>

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/front_layout"
            android:layout_width="300dp"
            android:layout_height="600dp"
            android:layout_gravity="center"
            android:background="@drawable/photo_ticket_border_line"
            android:visibility="@{viewModel.spinBackSide ? View.GONE : View.VISIBLE}"
            app:layout_constraintVertical_bias="0.412">


            <ImageView
                android:id="@+id/solaroid_photo"
                android:layout_width="match_parent"
                android:layout_height="400dp"
                android:layout_marginLeft="24dp"
                android:layout_marginTop="28dp"
                android:layout_marginRight="24dp"
                android:adjustViewBounds="true"
                android:scaleType="centerCrop"
                android:src="@drawable/tmp_photo_image"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintHorizontal_bias="0.489"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"

                tools:src="@tools:sample/avatars" />

            <TextView
                android:id="@+id/solaroid_text_date"
                android:layout_width="300dp"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:text="@{viewModel.photoTicket.date}"
                android:textAlignment="center"
                android:textSize="18sp"
                android:textStyle="italic"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintHorizontal_bias="0.492"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/solaroid_photo"
                app:layout_constraintVertical_bias="0.062"
                tools:text="2022.01.18" />

            <TextView
                android:id="@+id/solaroid_text_front"
                android:layout_width="300dp"
                android:layout_height="100dp"
                android:layout_marginBottom="6dp"
                android:gravity="center"
                android:text="@{viewModel.photoTicket.frontText}"
                android:textAlignment="center"
                android:textSize="16sp"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/solaroid_text_date"
                tools:text="좋은 사람들을 만난 계양산에서" />


        </androidx.constraintlayout.widget.ConstraintLayout>

        <ImageView
            android:id="@+id/spin_photo"
            android:onClick="@{()->viewModel.onClickSpin()}"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:layout_gravity="top|right"
            android:adjustViewBounds="true"
            android:padding="5dp"
            android:layout_marginTop="15dp"
            android:layout_marginRight="23dp"
            android:scaleType="fitCenter"
            android:src="@drawable/spin_photo" />

    </FrameLayout>
</layout>